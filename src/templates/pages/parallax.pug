extends ../parallax.layout.pug

block variables
  - var title = 'Parallax'

block content
  #parallax.parallax
    - for (var i = 1; i < 9; i++)
        .parallax__layer
          img(src=`/assets/images/parallax/layer-${i}.png`).parallax__image